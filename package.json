{
  "name": "dfn-gap-analyzer",
  "version": "0.1.0",
  "description": "AI-powered manufacturing job routing and analysis for Nigerian factories",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"corepack pnpm --filter @dfn/frontend dev\" \"corepack pnpm --filter @dfn/backend dev\"",
    "build": "corepack pnpm --filter @dfn/shared build && corepack pnpm --filter @dfn/backend build && corepack pnpm --filter @dfn/frontend build",
    "test": "corepack pnpm -r --if-present test",
    "lint": "corepack pnpm -r --if-present lint",
    "type-check": "corepack pnpm -r --if-present type-check"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.16.1"
  }
}
